# Settlement by arrival — Pass 27 of the null experiments (2026-09-05)

Driver: `native/eval/the-fold/settlement-by-arrival.mjs` (0 model calls). Computation: `lib/settlement-by-arrival.mjs`. Enforcing test: `tests/settlement-by-arrival.test.js` reads every number below (P94/P95); this document is a transcription.

**Configuration.** Production reader, recipe `9c70723fe2b4`; the product assay's built corpus (A states the opening, B denies it) read on arrival; the door lands the contest (S69). Three arrivals: C restates the link exactly; D is silent on the pair; E names both ends without stating the relation. The seeker (`contestedSearch`) is run with the door's own `NEEDS_THIRD_SOURCE` and a DECLARED featurizer that keeps numerals.

**Numbers.** After A and B: 1 contest, kind `contest`, disputed by `northgate-b.txt`. C restates: sources 2 (corroborated), still disputed by B, 1 contest open — corroboration is a label, never a gate (P89). D silent: 1 open, 0 candidates. E co-present: seeking 1, candidates `["northgate-e.txt"]`, stating `[A]`, contradicting `[B]`, 1 contest open after the search — a lead, never a landing. Controls: kinds `[individuation]` → 0 seeking, 1 unrouted with its reason; kinds undeclared → refused. `settleDispute` UPHELD: timeline `link → cut → contest → settled`, standing `{link: live, cut: live, contest: settled}`, `disputedBy []`, no concession. CONCEDED: the concession handed back for the link; after conceding, `link → cut → contest → settled → conceded`, `{link: conceded, cut: live, contest: settled}`, the link gone from the fold.

**Found by running.** `textFeatures` drops numerals and closed-class words, so a note whose second end is a bare date (`in 1889`) has NO features under the default featurizer and `thirdSourceCandidates` can never find a candidate for it (`defaultCandidates: 0`). The organ injects its featurizer, so the driver declares one keeping 3–4 digit numerals and reports the default's blindness beside the number rather than around it. Whether the default should keep numerals is a decision for the seeker's own pass (the assay's date-shaped ends are common in real material).

**What this licenses.** Arrival alone never settles a contest; a third source is a candidate for the seeker or nothing; only a recorded settlement with its outcome and trigger closes it, and the timeline reads every act at its seq. **What it does not.** The settlement here is declared by the driver; the witness tier's own settlement on a real third source is unrun (the seeker arm), and the numeric-end blindness is disclosed, not fixed.
