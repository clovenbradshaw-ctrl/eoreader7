# Re-zero under a stream — Pass 24 of the null experiments (2026-09-05)

Driver: `native/eval/the-fold/void-rezero-stream.mjs` (0 model calls). Computation: `lib/void-rezero-stream.mjs`. Enforcing test: `tests/void-rezero-stream.test.js` reads every number below on each suite run (P94/P95); this document is a transcription.

**Configuration.** Production reader (12 organs declared, recipe `9c70723fe2b4`); the product assay's built corpus streamed one sentence per arrival (7 passages); four voids declared BEFORE reading with scope `{sources: all 7, read: 0, total: 7}`; 20 seeded shuffles.

**Forward order.** `opened-any` filled@3 by `the northgate observatory|opened|in 1889` · `repaired-any` filled@4 · `opened-1889` filled@3 · `closed-never` open, reached true (7 of 7) — its timeline `declared → redeclared` (the end-of-stream declaration is its own event). The cut ("never opened in 1889") was heard at cursor 6 and filled nothing.

**Denial first.** With the denying passage moved to the front: cut heard at cursor 1; `opened-1889` filled at cursor 4, when the link arrived — a denial is not an arrival of the thing.

**20 shuffles.** Final open set identical in every order: `{closed-never}`. Fill cursors moved with the order (5 distinct cursors for each filled void); the never-stated void was open in all 20.

**Controls built to fail.** Truncated at 2: every void open with `reached: false` (2 of 7) — a fact about the reader, typed apart from the finding. Deranged corpus: filled `{opened-any, repaired-any}` against the real `{opened-1889, opened-any, repaired-any}` — the exact void is what the derangement loses.

**What this licenses.** A void is an event with a cursor: it fills at the first stating arrival, never at a denial, and the open set at the end of a read is order-invariant while the cursors are not. **What it does not.** Seven sentences of built material; no paraphrase (the void's ends must match the arrangement's ends by note identity — the same wall corroboration has); no live page run.
