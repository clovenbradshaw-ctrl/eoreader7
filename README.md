# EOReader 7

EOReader 7 begins from the frozen EOReader 6.1 cut at `e20e441d3cdfb735d605c75037e6d73892e707c0`.

## Transitional bootstrap

The source organism is pinned as the `legacy-eoreader6.1` submodule. Root compatibility symlinks expose the same paths consumed by existing applications while v7 is progressively made native around `packages/kernel`.

Clone with submodules:

```sh
git clone --recurse-submodules https://github.com/clovenbradshaw-ctrl/eoreader7.git
```

This is deliberately a migration baseline, not the target architecture. A path may move out of the legacy submodule only when its native v7 replacement passes the existing conformance and The Fold compatibility contracts.

Canonical reading remains:

`Fold -> Orientation -> Encounter -> Perception -> Witness -> Interrogation -> DeltaFold -> revised Fold`

Surprise, tension, and release remain derived dynamics. The EO cube remains complete at 27 addresses.
