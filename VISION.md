# The Fold — end-state vision

*A folded, revisable log — not a fixed spec. Source of truth: `documents/vision-end-state:1.jsonl` (append-only, git-tracked specifically so every clone and every scheduled routine resumes the SAME ledger). This file is a rendered snapshot of that ledger's current fold, regenerated 2026-09-26, not hand-written. Superseded revisions stay in the ledger; only the surviving lines are rendered below.*

## What it can do

Absorb any arbitrary content — text, code, music, a legal filing, a scraped record, any modality — and read it, not summarize it, into one lossless, addressable structure: the EOT/GFP cube, the same taxonomy regardless of what went in. Every referent in that structure is a monad, "for someone," carrying its own universe rather than being a bare string; one address refers losslessly to the whole thing it names.

At any point in that structure — any cursor, any referent, any moment — answer "what is this, here, given these priors, given this other selected content" by returning the fold: the addressed, provenance-carrying state of everything relevant at that point, with atmosphere (the ground already stood in), lens (what's being aimed at), paradigm (the frame willing to be revised), and significance (how much this matters, measured against a real null, never asserted) attached.

Generate, when the ask calls for producing something new, by rendering the fold rather than asking a model to invent past it: restructure at the GFP/SVO level (parse to structure, address it, edit only what's actually revisable, reassemble), never rewrite freely. The model is the mouth — it renders what the structure has already, mechanically, licensed; it never supplies content the structure hasn't earned.

Run two tiers by design, not by accident: a fast, cheap, always-on System 1 (a small parser, a perceptron, whatever is fast enough to run on everything) that gets most of it right through sheer statistical concentration of ordinary usage, and a slow, deliberate, context-relative System 2 (ablation-grain-pressure's shape: embed with and without the token in question, measure the delta, weigh it against real provenance-tagged distributions, return a parliament of votes and a revisable pressure, never a verdict) invoked exactly where System 1 admits it doesn't know — never where a model is simply asked to guess.

Do real work in the world with this: civic and legal accountability (Eviction Overwatch, CaseLink, MNPD network audit, ALPR abuse detection) where the grounding discipline (Mozi: it is in the bytes the eyes and ears can witness, or it isn't) is the actual product, not a decoration on top of a language model.

Keep every organ separately owned and named (the Handle system, the archon-holocracy registry) so no one session or one person has to hold the whole thing in their head, and so "who fixes this" is always answerable by checking a real registry, not by guessing or by defaulting to whoever's in the room.

> Synthesized from this session's own work and the project's standing memory (referent-model-not-pointers, holonic-objects-in-slots, fold-instant-cursor-vision, fold-s1-s2-parallel-models and its second instance in the parser, modality-transfer, restructure-not-rewrite, the atmosphere-lens-paradigm essay, the archon-holocracy registry) — recorded, not invented, on the user's own direction: 'create a vision of the end state.'

## What it can never claim to do

Never claim an objective, final category for any token, referent, or claim. Every settled tag is a high-confidence default on a continuum, not a different epistemic class from a typed gap — "shed" and "dog" are the same mechanism at different points on the same distribution, not a hard case and an easy case. A system that forgets this and starts asserting fixed kinds has stopped being this project and become the thing it was built to refuse.

Never close the ladder. This project's own repeatedly-rediscovered shape (the wheel that doesn't close, the ring that recurs at rising altitude, the MHC order-13 true negative, the atmosphere-lens-paradigm essay's own thesis) says a final, complete, "done" version of this system would be exactly the false objectivity everything else here refuses. There is no version of this vision that reaches "finished, no further revision needed" — that outcome is excluded by the same logic that makes the rest of it honest.

Never fix a real ceiling with more of the same lever. Measured this session: gold-based retraining transferred on 1 of 4 newly-targeted patterns even when built carefully to generalize — supervised fixing has a real, now-quantified ceiling, not just a theoretical one. And a whole construction class (reported-speech with an interrupted, coordinated subject) has no projective UD analysis at all — the parser architecture cannot represent the correct answer regardless of training data. When a ceiling like that is found, the honest move is to name it and change the approach, not spend another round of the same fix hoping for a different result.

Never let a model act as a groundless oracle. Not Claude, not a bigger local model, not "ask it and see" — a model may render what a mechanical structure has already licensed, or it may propose a bridge that gets mechanically verified before being kept, but it may never be the thing a judgment rests on with nothing checking it. This is the line this whole session kept finding on the wrong side of: a fabricated UD citation, an invented speech act, an adversarial verifier confidently rejecting a rule that turned out to be correct once actually checked against real documentation. The rule exists because the failure mode is real and was caught happening, more than once, in this exact session.

Never build in a vacuum. No new archon gets spawned, no ownership gets declared absent, without checking both naming systems (the Handle table and archon-holocracy/archons.json) first — lesson #75, paid for by getting it wrong once already. Work on any piece of this vision routes through whichever archon already owns that piece, or names the gap honestly if none does, rather than one session freelancing across all of it.

Never claim 100% on anything measured against real, open-ended material. Not parser accuracy, not summary fidelity, not pocket-discovery precision. The right number to report is always the honestly measured one, including when it's a partial win, a null result, or a discovered ceiling — never a rounded-up claim of completeness.

> Same synthesis basis as vision-can; the two are deliberately written as a pair (what it does / what it will never claim to do) per the user's own framing.

## How this gets chased

This ledger is the mechanism, not a description of one: append-only, each revision superseding the last without erasing it, exactly the discipline already used for every generated piece this session. Anyone — any session, any archon, any scheduled routine — reads the current fold of THIS document the same way any other document in this pipeline gets read, and appends a revision when something changes, rather than editing a fixed spec in place.

Ownership, checked against the real registries before any new work starts (per lesson #75): Chomsky (native/kernel/universal-grammar.js, eot-rich.js, english-parser.js, clause-tense.js) owns the parser/syntax substrate this session pushed on directly. Sullivan (adapters/text/morph-cues.js, existence-grain.js, eval/lavar/sullivan-*.mjs) owns morphology-by-elimination and the period-parse measurement that the parser's period-blindness should have been checked against sooner. LaVar (LAVAR.md, eval/lavar/) owns grading the reader against real material and keeping the mistake corpus this project's own honesty depends on. Grounding (organs/grounding.js, ground-cite.js — Mozi) owns verifying any claim in this vision, or built from it, against real bytes. No current archon owns ablation-grain-pressure.js's live wiring or the cursor-addressable fold interface itself — both are named gaps, not silently assigned, until an archon is actually spawned for them following the Handle-table-plus-registry check.

The concrete next actionable steps, in the order this session's own findings license them, not in order of ambition: (1) unblock the shared Ollama /api/embed contention so build-ablation-grain-prior.mjs can complete a real run — infrastructure, not code. (2) Run eval/the-fold/ablation-pressure-calibration.mjs for real once centroids exist, and only wire ablationPressureFor live if that calibration actually licenses per-case trust, per the standing rule already in the code. (3) Replace this session's ad hoc agglomerative pocket-discovery with a properly-powered, established method (the gap statistic or a real mixture-model comparison) before trusting pocket counts on real data — the synthetic tests this session ran found the ad hoc version underpowered, honestly, not fixed. (4) Only after 1-3: begin the actual cursor-addressable fold interface — the piece that has been described three times this session and built zero times.

REVISION, same session, minutes later: the user asked for this to stop being a description and become a standing process -- committed to the repo, and a scheduled Claude Code routine (model: sonnet) set up to actually resume this work on a recurring cadence, archon ownership checked each time before any new task is claimed, rather than left as a one-time write. That routine's own config and its first scheduled run are the next two lines in this ledger, appended right after this one -- read those for what's actually standing, not just planned.

REVISION, 2026-09-25/26: step 3 of the ordered next-steps list above is done -- native/adapters/text/pocket-discovery-gap.js implements the gap statistic (Tibshirani, Walther & Hastie 2001) replacing the ad hoc agglomerative-z-test approach, whose own honest earlier measurement this session found underpowered (1/5 true positives on realistic 3-cluster synthetic data). Measured on the SAME synthetic scenarios: 4/5 true positives (vs 1/5) at a cost of 1/5 false positives on null data (vs 0/5) -- a real, reported precision/recall trade-off, not an unqualified win. 5 node:test cases pass (native/adapters/text/pocket-discovery-gap.test.mjs). NOT wired into ablation-grain-pressure.js's buildCentroidsFrom -- steps 1-2 (unblock the shared Ollama /api/embed endpoint, run the real calibration) remain blocked; confirmed live via a 15s curl against http://127.0.0.1:11434/api/embed returning STATUS:000 immediately before this step, same failure mode observed all session. Checked both archon-ownership registries first (README.md Handle table, archon-holocracy/archons.json) -- neither names an owner for this domain, consistent with vision-routines' own prior disclosure of it as a named, undecided gap; no new archon was spawned for it this step. Committed as a standalone, tested module ahead of its dependency clearing, per the ledger's own instruction to do real, bounded, currently-licensed work rather than wait on a still-blocked step.

REVISION, 2026-09-25/26 (same 30-min-burst session): step 4 of the ordered next-steps list is BEGUN, not finished -- native/the-fold/fold-at.js is the first real slice of the cursor-addressable fold interface described repeatedly in this project's memory and built zero times before this file. foldAt(address, claims) partitions a flat GFP claim set into here/ancestors/descendants relative to a holon-address cursor, reusing kernel/gfp-claim.js's own holon()/contains()/ancestry() unmodified -- real, new plumbing, 6 node:test cases passing. The three richer layers the full vision describes (atmosphere, paradigm, significance) are deliberately left as typed gaps ({wired:false, reason}), never fabricated: atmosphere and paradigm each name a real existing module (kernel/atmosphere-math.js, the-fold/paradigm.js) this step did not wire because their real input shapes (an obligations sequence; a learned paradigm object) were not verified against a live caller; significance has no implementation anywhere in this repo at all, confirmed by grep before writing. Next bounded steps this names, not yet started: verify atmosphere-math.js's real obligations-sequence contract against a live caller and wire it in; verify paradigm.js's learnParadigm/evaluateParadigm contract and wire it in; design and build a real, null-measured significance function (no hand-set threshold, per this project's own standing rule) where none exists today. Checked both archon-ownership registries first (README.md Handle table, archon-holocracy/archons.json) for cursor/fold-at/foldat -- neither names an owner; no new archon was spawned for it this step, consistent with vision-routines' own prior disclosure of this domain as a named, undecided gap.

REVISION, 2026-09-26 (same burst session, continuing on user direction 'keep going, don't stop'): fold-at.js's atmosphere layer is now genuinely wired -- checked two real, independent production call sites first (kernel/emergent-terrain.js's atmosphereProjection, kernel/theory-of-mind.js, two separate call sites), confirmed they agree on the same obligations shape ({id, grounds, alternatives, consequences, persistence, openedAt, constraint}), then called the real interpretiveAtmosphereFactorField directly. When a caller supplies obligations, foldAt returns its real computed field; when none are supplied, atmosphere is wired:true with field:null (honestly "nothing to compute here", not "not wired"). 8 node:test cases pass (2 new, exercising both the empty and real-obligation paths). Also corrected this same ledger's own prior claim about paradigm: checking evaluateParadigm's real callers this step found ZERO outside its own test file (every real the-fold caller only ever BUILDS a paradigm via learnParadigm/learnParadigmEmergent, never evaluates a candidate against one) -- there is no live contract to verify against, so paradigm stays an honest, now more precisely worded, typed gap rather than being wired from a guess. significance remains unimplemented anywhere in the repo, re-confirmed.

REVISION, 2026-09-26 (same burst session, user: 'go, dont stop, that's the whole point of this'): corrected a FALSE claim this same file's own earlier revision made -- "no significance-measurement module exists anywhere in this repo" was wrong. Real, already-committed modules exist: kernel/bayes-surprise.js and kernel/consequential-surprise.js, both with real production callers (the-fold/expertise.js, form-prior.js, paradigm.js, learn-pass.js; kernel/settling.js, contextuality.js; organs/archon-compendium.js, capacities.js; docs/surprise-organs.js). Wired the part that fits foldAt's own PURE contract: bayes-surprise.js's predict() (read-only -- checked that admit() mutates the holograph and predict() does not, before choosing predict) computes real Bayesian surprisal in bits over slot/value facts derived from the claims at a cursor, when the caller supplies a holograph. 11 node:test cases pass total (4 new, including one that specifically proves the caller's holograph is never mutated). Left consequential-surprise.js's load-bearing/local partition as a precisely-reasoned gap -- it needs a dependents index and a seedsOf function whose real-world usage (checked via docs/surprise-organs.js) is domain-specific and does not generalize to holon-addressed claims without original design work not attempted this revision.

REVISION, 2026-09-26 (same burst session, user asked 'how close are we to arbitrary generation'): checked whether fold-at.js (built this session) connects to the nine-stage generation pipeline (native/the-fold/pipeline-run.mjs: void/surf/shape/hunt/skeleton/loop/pathos/piece). It does not -- confirmed by grep for any import of fold-at.js or any call to foldAt( anywhere under native/ outside fold-at.js and its own test file: zero matches, after catching and redoing a sloppier first grep that had produced a false hit on an unrelated function (proposition-holograph.js's foldAtom).

Looked for the most likely real integration point: hunt.js's huntGround(), since its name suggested it might already work with GFP-style ground addresses. It does not -- huntGround's "ground" is a different, unrelated concept (evidentiary source material admitted by topical relevance: naming a being of the subject, or carrying a topic word), a false friend of gfp-claim.js's holon-path "ground" that foldAt addresses. Wiring foldAt into the pipeline on the strength of shared terminology alone would have been exactly the guessed-composition mistake this session has repeatedly caught itself making and corrected (paradigm's contract, significance's non-existence claim) -- so it was not attempted. Real integration requires reading each pipeline stage's actual data model to find where a genuine holon-addressed claim set already exists or naturally could, which this step did not have room to do carefully. Named as the concrete next step, not guessed at: before wiring foldAt into generation, first identify which pipeline stage (if any) already produces or consumes GFP claims with real holon grounds, as opposed to the pipeline's own paragraph/text-based addressing.

REVISION, 2026-09-26 (same burst session, continuing 'keep going'): the prior revision correctly found hunt.js's "ground" was a false friend, but searched too narrowly before concluding no real integration point exists. Widening the check (grep for gfp-claim.js usage across every stage file pipeline-run.mjs imports) found one: native/the-fold/arrange.js imports renderGeneralization from kernel/gfp-claim.js, and arrange.js itself imports thesisBasin/thesisGeneralization from native/the-fold/thesis-claim.js -- which is the real source. thesis-claim.js builds real GFP claims with genuine holon grounds keyed to the piece's own structure: `claimFromTriple(note.end1, rel, note.end2, { ground: holon(\`/\${f.pt.part}\`), ... })` (line 136) -- one holon address per piece-part, exactly the addressing scheme foldAt consumes, not a false friend this time: verified by reading the actual line, not inferred from a name. This is confirmed, not yet wired -- thesisGen.claims is a real, holon-addressed claim set that a real caller (arrange.js) already holds at generation time; foldAt(someCursor, thesisGen.claims) would be a genuine, non-guessed integration once arrange.js's own ~650-line control flow is read carefully enough to find the right place to call it and use the result for something real (not a decorative addition). That careful read is the concrete next step, named rather than rushed into within this same burst.

REVISION, 2026-09-26 (same burst session, same 'keep going' thread): reading arrange.js's own actual control flow (not just its imports) around thesisGeneralization found a real, important caveat to the immediately prior revision. arrange.js's own comments report: "Measured 2026-09-25 across NINE real grounds — a house doc, two UD treebank READMEs, an OHS audit, a Call of the Wild excerpt, UK legislation, a SCOTUS oral-argument excerpt, the surf white paper, the Cumberland essay: zero basins validate" and, restated later in the same file, "Measured 2026-09-25 across nine real grounds (above): zero basins fired." The thesis-generalization branch that produces holon-grounded GFP claims (thesis-claim.js) is real code with a real, verified contract, but is ALSO measured, by this repo's own prior work, to never actually activate on the nine real documents tested so far. Wiring foldAt to thesisGen.claims would be technically correct and would still be honest, verified plumbing -- but by the repo's own numbers it would have nothing to act on in the overwhelming majority of real runs. This is not a reason to abandon the integration (the mechanism firing is itself measured, not assumed broken -- refrains DO trigger it per the same comments), but it tempers "found the real integration point" into "found a real, rarely-populated one" -- an honest correction, not a reversal. No wiring was attempted this revision; the next step is unchanged (arrange.js's full control flow, read carefully) but now carries this caveat forward rather than an unqualified "this is the answer."

REVISION, 2026-09-26 (scheduled cron firing, step chosen per the ledger's own named next-step): built native/the-fold/arrange.js's claimsFromFeat(feat), the general bridge from arrangeEssay's own always-run per-point extraction (feat, notesOf -- run over EVERY point in EVERY arrangeEssay call, unconditionally, verified by reading points' construction) to real, holon-addressed GFP claims, superseding the near-dormant thesis-generalization path as the practical integration point. Reuses notesOf's own already-computed subject/relation/object triples and declared polarity via claimFromTriple; notes with notesOf's own unresolved ('?') polarity state are excluded and counted separately, never coerced into a guessed '+'/'-' (gfpClaim's own validation only accepts those two, checked against its source). A pure addition -- no existing line in arrange.js changed. 6 new tests pass, including a genuine cross-file end-to-end check (claimsFromFeat's own output fed directly into the real foldAt(), confirming a parent-part claim surfaces as an ancestor for a child cursor) -- not mocked, not asserted, run. Full existing test suites (arrange-falsify.test.mjs's 32 cases, fold-at.test.mjs's 11) re-run and still pass: this addition changed nothing else. Re-tested Ollama at the top of this cycle: still STATUS:000, steps 1-2 remain blocked. Checked both archon-ownership registries for arrange.js before touching it: neither names an owner. The concrete next step this names: run claimsFromFeat against a REAL arrangeEssay call on real material (not just synthetic feat shapes) to measure how often it actually produces claims on real documents, the way thesis-claim.js's own dormancy was measured -- not yet done this revision.

REVISION, 2026-09-26 (scheduled cron firing, the exact next step obs:12 named): ran claimsFromFeat against REAL arrangeEssay output on real prose, not synthetic feat shapes, through the actual pipeline (buildDraft/attachReferents/loadEotParser/attachEot/arrangeEssay). Measured on three real documents already used in this session's own summarization pilot: gpgp.md (Wikipedia, informational) 31 claims from 52 points, 0 unresolved; gift-of-the-magi.md (O. Henry, narrative) 58 claims from 140 points, 3 unresolved; war-and-peace-ch45.md (Tolstoy, translated 19th-century prose) 14 claims from 44 points, 1 unresolved. All three real, nonzero, non-degenerate -- confirming claimsFromFeat is commonly populated on real material, the opposite of thesis-claim.js's own measured zero-activation result. Exposed claimsFromFeat's output on arrangeEssay's own return value (a new `claims` key, additive only -- checked for any deep-equality assertion against a whole outline object anywhere in the repo before adding it; none found) so a real caller can reach it without reaching into arrangeEssay's function-local `feat`. Locked in as a committed, reproducible test (claims-from-feat-real.test.mjs, a real public-domain excerpt embedded directly rather than depending on this session's own scratchpad paths, unlike the pre-existing native/eval/the-fold/drive-summary-experiment.mjs's own default file paths -- a fragility noted, not fixed, this revision). Full existing suites re-verified: 44 tests across arrange-falsify.test.mjs, claims-from-feat.test.mjs and fold-at.test.mjs still pass. Re-tested Ollama at the top of this cycle: still STATUS:000, steps 1-2 remain blocked. The concrete next step this names: something must actually CALL arrangeEssay's new claims field and DO something with it via foldAt (atmosphere/significance) that changes what the pipeline produces -- until then this is verified, reachable data, not yet a capability.

REVISION, 2026-09-26 (scheduled cron firing): before wiring foldAt's output into anything that shapes generated text (obs:13's own named next step), tried calling foldAt against the real claims measured last cycle to see what it actually returns. Found ancestors:0, descendants:0 for every real cursor tried against gpgp.md's 31 claims -- the ground claimsFromFeat builds, holon(`/${f.pt.part}`), is FLAT (e.g. '/p18'), so every claim sits as a sibling of every other at the same depth; foldAt's ancestor/descendant machinery had nothing to find, regardless of how much real content existed. Checked eot-draft.js's own point construction (line 187) and found every point already carries a real, pre-existing hierarchical `path` field (`whole/p${i+1}/${j+1}`, e.g. 'whole/p4/3') that arrange.js's own points construction (line 121, `{...pt, part: p.id}`) already preserves onto every feat entry as `f.pt.path` -- unused until now. Fixed: claimsFromFeat's ground is now `holon(f.pt.path ?? \`/${f.pt.part}\`)`, using the real hierarchy with a fallback for any caller whose feat entries lack `path`. Re-ran the same live inspection against gpgp.md: grounds are now genuinely nested ('/whole/p13/3', '/whole/p16/2', ...), confirmed real, not just plausible-looking.

Disclosed, not solved: even with the real hierarchy, ancestors/descendants for a point-level cursor remain empty in this design, because no claim is currently grounded at the coarser part ('/whole/pN') or whole-document level -- points are real siblings under a shared part, not ancestor/descendant of each other, and foldAt has no separate "siblings" category. Getting real ancestor context (not just a correct address) needs either claims at multiple granularities or a sibling-aware fourth category in foldAt itself -- named as the next concrete step, not attempted this revision. 2 new tests lock in the fix (path used when present; flat fallback unchanged when absent); full suite re-verified at 47/47 passing across claims-from-feat, claims-from-feat-real, arrange-falsify and fold-at test files. Re-tested Ollama at the top of this cycle: still down, steps 1-2 remain blocked.

REVISION, 2026-09-26 (scheduled cron firing, the exact gap obs:14 named): added a fourth, purely structural category to foldAt -- siblings: claims sharing the cursor's own parent holon (one level up, via gfp-claim.js's own already-exported segmentsOf/holon, no new addressing concept). This directly answers what obs:14 found and disclosed: real per-point claims from claimsFromFeat are true siblings of each other under a shared part, not ancestor/descendant, and foldAt had no category for that relationship. Re-ran the same live inspection against gpgp.md's real claims that found the original gap: a cursor at /whole/p16/2 now correctly returns /whole/p16/1 as a real sibling -- confirmed on real data, not just the synthetic fixture. parentOf(h) returns null for the root ('/' has no parent), so the root cursor correctly has zero siblings, a real structural fact rather than an arbitrary special case. 3 new tests added (sibling inclusion, no double-counting an ancestor as a sibling, root has zero siblings); full suite re-verified at 49/49 passing across fold-at, claims-from-feat, claims-from-feat-real and arrange-falsify test files. Re-tested Ollama at the top of this cycle: still down, steps 1-2 remain blocked. The concrete next step this names: foldAt now has real, populated here/ancestors/siblings on real material -- the next honest question is whether wiring this into a generation-shaping stage (finish.js, loop-check.js) actually improves anything measurable, since no existing gap in either file currently names a use for it (checked two cycles ago); that measurement, not another foldAt feature, is what's actually owed next.

REVISION, 2026-09-26 (scheduled cron firing, pursuing obs:15's own named question): before writing more foldAt code, tried to find a REAL, already-motivated use for its siblings/ancestors in a generation-shaping stage, rather than inventing one. Read three candidate files' actual logic (not just names or comments): hunt.js's huntGround (already found, two cycles ago, to be a false-friend "ground" concept -- evidentiary source material, unrelated); finish.js's takesUp (checks whether a part's opening sentence "takes up" a being or word from the previous part's closing sentence -- works entirely through referent resolution and word overlap on raw text, never touches a GFP claim); loop-check.js's measurePiece/judgeLoop (measures whether a loop preserved anchors and answered the task's questions -- works through anchorsFor/carries on the piece's own text, never a GFP claim either). None of the three has an unmet need that foldAt's holon-addressed claim context would fill; each already solves its own adjacency/coherence problem through a different, working, non-claim-based mechanism. Wiring foldAt into any of them now would be manufacturing a use for it, not fulfilling one already there -- exactly the invented-purpose mistake this session's own standing rule (measure, don't assume) exists to catch.

This is not a dead end for foldAt, but it does redirect the real next step: either (a) foldAt's value is as a genuinely NEW capability (an interactive query/inspection surface over already-generated material, not a silent improvement folded into logic that already works its own way), which has never been attempted and would need its own honest scoping, not a forced retrofit; or (b) a deliberately DESIGNED experiment -- pick one specific generation decision, build a foldAt-informed alternative, and measure it against the current approach on real material -- which is a real, bounded project of its own, not something to back into by searching existing files for an accidental fit. Which of these two paths is worth pursuing is an open decision this revision names but does not make.

REVISION, 2026-09-26 (scheduled cron firing, pursuing obs:16's own named option (a)): built cli/fold-at.mjs, the first time anything in this repo lets a person actually run a command and ask "what is the fold at this point" of a real document, rather than only exercising the backend through a test or a throwaway script. Runs the real pipeline (buildDraft, attachReferents, loadEotParser, attachEot, arrangeEssay) on a file argument, then the real foldAt(address, outline.claims), printing here/ancestors/siblings/descendants and each layer's real state -- a computed field when wired, the exact honest gap reason string when not, never a fabricated summary. A `list` address mode prints every real distinct holon address the document actually produced, so a user finds a real cursor rather than guessing one. Verified against a real document (gpgp.md): 31 claims, 31 addresses listed correctly; querying /whole/p16/2 correctly returned its real sibling at /whole/p16/1 and honest gap text for significance and paradigm. 3 process-level tests (spawning the real CLI, not importing its internals) lock this in, covering usage/exit-2, list mode, and a real address query's full output shape. This is the "genuinely new capability" path named as option (a) two cycles ago, chosen over forcing a retrofit into pipeline stages already found to have no motivating gap for it.

REVISION, 2026-09-26 (scheduled cron firing): the CLI's significance output previously always printed "gap: no holograph supplied" since nothing built one. Fixed: cli/fold-at.mjs now builds a real bayes-surprise.js holograph, admitting (via the real, documented-mutating admit()) every OTHER claim in the document (excluding the queried cursor's own, to avoid a self-fulfilling low score), using the same slotsFromClaims() shape foldAt itself already uses internally (exported from fold-at.js for this purpose -- a one-word, no-behavior-change export). foldAt then reports the cursor's real surprisal against this real, document-derived prior via its own existing predict()-only path (never re-admitting).

Honestly measured, not just built: tested against gpgp.md's 4 different real cursors -- significance came back as either 16.54 or 17.54 bits, only two distinct values across four tries. The likely cause, checked by reading slotsFromClaims' own slot-naming: every claim's role fillers compete in the SAME small set of shared slots (rel, role:ARG0, role:ARG1, polarity) across the whole 31-claim document, and most specific fillers (proper nouns, specific nouns) are unique to the document, so the "novel value" cost in bayes-surprise.js's own Bayesian model dominates almost uniformly regardless of which claim is queried. This is a real, working number, not fabricated -- but it does not yet meaningfully discriminate "surprising" from "unsurprising" content on a corpus this small, and should not be presented as more informative than it is. 2 new tests (fold-at.js's export, the CLI's real-number output) plus the full existing suite re-verified passing (16 fold-at + 4 cli tests). Re-tested Ollama: still down.

The next honest question this names: does significance become more discriminating on a larger real document (war-and-peace-ch45.md, or the full text rather than an excerpt), or is a coarser slot design (grouping by relation LEMMA across claims, not per-claim-instance) needed regardless of size -- not yet tested, named as the next step rather than assumed either way.

REVISION, 2026-09-26 (scheduled cron firing, resolving the exact question obs:18 named): tested cli/fold-at.mjs's significance output against a larger real document (gift-of-the-magi.md, 58 claims) instead of the smaller one (gpgp.md, 31 claims) that showed only two distinct values (16.54/17.54 bits) last cycle. Sampled 10 real cursors: significance spanned 11.51 to 19.83 bits, an 8.3-bit range -- genuine variance, not a constant. Checked causally, not just numerically: the lowest-scoring cursor ("she do it", 11.51 bits) uses common pronoun/verb words repeated throughout the story; the highest-scoring cursor ("hair fall rippling", 19.83 bits) uses distinctive, unique descriptive words -- exactly the expected Bayesian-surprise behavior (common = unsurprising = low bits, rare = surprising = high bits). This resolves the open question: the near-constant values measured last cycle were a real corpus-size effect (a 31-claim document has too few repeated relations/fillers for the prior to differentiate against), not a flaw in the slot design -- no code change was needed or made this revision, only the measurement itself, which the ledger's own last revision correctly identified as the thing actually owed.

The concrete next step this leaves open: significance is now confirmed real, working, and meaningfully discriminating on real material of sufficient size -- the same three named gaps from earlier revisions remain unaddressed (paradigm has no live caller to verify against; consequential-surprise's load-bearing/local partition needs a seedsOf design for holon-addressed claims; foldAt has never been tried on a document larger than ~140 points). Any of these, or a genuinely new direction, remains open for the next cycle -- none is uniquely licensed by this revision's own finding.

REVISION, 2026-09-26 (scheduled cron firing, picking one of the three open options from obs:19 -- consequential-surprise.js's own named gap): built native/the-fold/claim-dependencies.js, the missing seedsOf/dependents-index design for wiring kernel/consequential-surprise.js's load-bearing/local significance partition into holon-addressed GFP claims. Before building, checked ownership carefully and found a real, non-obvious false-friend: an archon's mission mentions "cascade," but its own text names native/kernel/prior-query.js's unrelated "meaning-potential cascade" (a chain of prior-family lookups), not kernel/cascade.js (BFS reachability over a dependency graph) -- confirmed by reading prior-query.js directly; kernel/cascade.js itself has zero references in either registry, genuinely unowned.

The dependency relation: two claims are connected when they share a role filler (the same ARG0 or ARG1 value) -- a mechanical proxy for shared-entity/coreference connection, consistent with how this project already treats shared referents elsewhere (finish.js's takesUp, referents.js's resolved beings), not a newly invented notion. claimDependencyIndex(claims) builds kernel/cascade.js's own dependentsIndex from this relation; seedsOfClaimFiller(slot, value) seeds only from role-shaped slots (matching fold-at.js's own slotsFromClaims naming), never from rel or polarity, which carry no shareable entity. 4 tests pass, including one against real claims from gpgp.md confirming real role fillers are genuinely shared across more than one real claim, not just true on synthetic data. Full existing suite re-verified: 25 tests across fold-at, claims-from-feat and cli/fold-at, no regressions.

Built and tested standalone only -- NOT yet wired into fold-at.js's significance field or the CLI, which remains the next concrete step: call consequentialSurprise(holo, facts, {index, seedsOf: seedsOfClaimFiller, ...}) inside foldAt when the caller supplies an index (built via claimDependencyIndex), alongside the existing predict()-only path, and measure whether it produces a genuinely different (and genuinely load-bearing) partition on real material -- not attempted this revision. Re-tested Ollama: still down.

REVISION, 2026-09-26 (scheduled cron firing, completing the wiring named as the concrete next step last cycle): fold-at.js's significance field now carries a real significance.consequential sub-field, wired to kernel/consequential-surprise.js via last cycle's claim-dependencies.js (claimDependencyIndex, seedsOfClaimFiller). One real conflict had to be resolved first: consequentialSurprise(holo, facts, ...) calls admit() INTERNALLY (confirmed by reading its source), which mutates the holograph -- unlike predict(), the only thing the existing significance path used, specifically to preserve foldAt's own tested purity guarantee. Resolved with a real cloneHolograph(holo) helper (deep-copying every field admit()/createHolograph() actually touch: schema, alpha, gamma, admitted, slots, absentMass, verified by reading both this revision) and running consequentialSurprise only on the clone. pValue is never defaulted (consequentialSurprise's own guard throws without one, and this project's standing rule already forbids hand-set thresholds) -- the layer activates only when the caller supplies holo, index, seedsOf, AND pValue together; any one missing stays an honest gap.

Verified end to end with a real ad hoc script before writing tests: three synthetic claims sharing "author" and "ocean" fillers produced real loadBearing:true rows, and the caller's original holograph's .admitted stayed at 0 throughout. 4 new committed tests lock this in (the gap case, the wired case with real rows, and two separate mutation checks -- one for the existing predict() path, one for the new consequential path), using the test file's own existing fixture claims (which already share "the-general"/"the-order" across two grounds) rather than new synthetic data. Full related suite re-verified: 33 tests across fold-at, claim-dependencies, claims-from-feat (both), and cli/fold-at -- no regressions.

Not yet done, named as the next step: the CLI (cli/fold-at.mjs) does not yet pass index/seedsOf/pValue, so its own printed output still shows the consequential gap even though the library now supports it -- wiring the CLI, and choosing/disclosing a real pValue rather than an arbitrary one, is the natural next increment. Re-tested Ollama: still down.

REVISION, 2026-09-26 (scheduled cron firing, completing the CLI wiring named as the concrete next step last cycle): cli/fold-at.mjs now takes an optional --pvalue N flag. When supplied, it builds a real dependents index over ALL of the document's claims via the-fold/claim-dependencies.js's claimDependencyIndex and passes {index, seedsOf: seedsOfClaimFiller, pValue} into foldAt, activating the already-real significance.consequential layer landed last cycle. Without --pvalue, the printed output stays the same honest gap as before -- the CLI never invents or silently defaults a pValue itself, matching consequentialSurprise's own guard and this project's standing rule against hand-set thresholds; --pvalue 5 (out of (0,1)) is rejected with exit 2 and a clear message, never clamped.

Verified against gift-of-the-magi.md at pValue=0.05 across 6 real cursors: most showed 0.00 load-bearing bits (locally surprising only), but two showed genuinely load-bearing results -- "she" (the story's protagonist, reaching 18 of the document's ids, rank 1.00) and "it" (reaching 7 ids, rank ~0.98-0.99) -- a real, meaningful signal: the pronoun referring to the protagonist is correctly identified as load-bearing where a locally-rare but isolated word is not. 2 new committed tests (real consequential output via --pvalue; out-of-range rejection). Full related suite re-verified: 26 tests across fold-at, claim-dependencies and cli/fold-at, no regressions. Re-tested Ollama: still down.

This closes the loop this session opened tonight: pocket-discovery (gap statistic) -> fold-at.js (holon addressing) -> atmosphere -> significance -> siblings -> the pipeline-integration search (a real negative result) -> cli/fold-at.mjs (the actual interface) -> real document-derived significance -> the variance question resolved -> claim-dependencies.js -> consequential-surprise wired in the library -> now wired all the way to the command line. What remains named and undone: paradigm still has no live caller to verify against; foldAt has never been tried on a document larger than ~140 points; and whether any of this changes what the generation pipeline actually produces is still an open, undesigned experiment, not yet attempted.

REVISION, 2026-09-26 (scheduled cron firing, resolving the other open item from obs:22 -- foldAt untried past ~140 points): ran cli/fold-at.mjs against a genuinely larger real document already in this repo (native/eval/the-fold/results/movies/fabre-chapters/fabre-chXIII-13.txt, 8941 words, ~4.6x the largest document tried before) rather than fetching new material. Real results: 185 claims from the real pipeline, sub-second (0.95s user time) even with the consequential-surprise layer active, and a genuinely correct load-bearing result -- "Philanthus" (a wasp genus Fabre's own chapter concerns), reaching 5 ids at rank 0.97.

A real, honest structural finding, not a bug: this document produced only ONE part ("/whole/p1/...", 185 points all under it), so a cursor's siblings came back as 184 -- effectively the whole document, not a meaningfully narrowed neighborhood. Checked mechanically, not guessed: the raw .txt file has zero blank lines (confirmed by grep -c "^$"), and eot-draft.js's own paragraph-splitting is blank-line-based -- this source's formatting, not a foldAt or claimsFromFeat defect, collapsed the whole chapter into one part. siblings degrades to near-useless exactly when a document lacks the paragraph-break convention the pipeline's part-detection depends on; the earlier documents (gpgp.md, gift-of-the-magi.md) all had real blank-line breaks and produced real, narrow sibling sets.

This resolves the scale question honestly: performance and correctness hold at ~4.6x the previously-tested size; the one real caveat found is about SOURCE FORMATTING, not the fold-at.js/claimsFromFeat machinery itself, and is worth disclosing to any future caller (a document without blank-line paragraph breaks will see degraded siblings usefulness) rather than silently assumed away. No code change was made or needed this revision -- the measurement itself was the owed work, same as the earlier significance-variance cycle. Re-tested Ollama: still down.

The two remaining named gaps stand unchanged: paradigm has no live caller to verify against; whether any of foldAt's real, verified machinery changes what the generation pipeline actually produces remains an undesigned experiment, not yet attempted. Both remain open for a future cycle or a new direction.

REVISION, 2026-09-26 (scheduled cron firing, resolving the last standing open item from obs:22 -- paradigm's missing live caller): read learn-pass.js's real, only production call site of learnParadigmEmergent (lines 190-229: instances/population built via toInstanceUnits/segmentCollection/elementsOf from fetched web documents) and paradigm.js's own unitFacts() implementation. Found the real "unit" candidate model is about DOCUMENT FORMATTING -- indentation, heading level and capitalization, block-breaks, sentence-final punctuation -- not claim content at all. This corrects the prior framing ("no live caller exists yet to verify against") into a more precise, final one: the real caller that exists operates in a genuinely different domain than foldAt's GFP claims (rel/roles/polarity/ground). paradigm.js answers "is this document's typesetting consistent with a learned form," not "does this claim hold" -- the same category of finding as hunt.js's "ground" and an archon's "cascade" mission, both false friends found earlier this session.

Updated fold-at.js's own header comment and the paradigm gap's reason text to state this precisely rather than leave the earlier, less-complete framing standing. This is not a gap awaiting future work -- wiring paradigm into foldAt would require inventing a claim-to-formatting-element mapping with no real basis, exactly the guessed-composition mistake this session has repeatedly caught and avoided. Full suite re-verified: 22 tests (fold-at, cli/fold-at), no regressions. Re-tested Ollama: still down.

With this resolved, only one open item remains from tonight's entire arc: whether any of foldAt's real, verified machinery (holon addressing, atmosphere, significance, consequential-surprise, siblings) changes what the generation pipeline actually produces is still an undesigned, unattempted experiment -- not a small next step, but a real project of its own, requiring a specific generation decision to be chosen and a real before/after comparison built. Named here as the standing frontier, not decided.

REVISION, 2026-09-26 (scheduled cron firing): re-checked the ledger's own original ordered next-steps list before picking a step, per this routine's own standing instruction. All four are now settled, not open: (1) unblock the shared Ollama /api/embed endpoint -- re-tested this cycle, still STATUS:000, genuinely blocked, not a code problem this session can fix from here. (2) run the real calibration once unblocked -- blocked transitively on (1). (3) replace the ad hoc pocket-discovery method -- DONE (gap-statistic, first commit of this burst). (4) begin the cursor-addressable fold interface -- DONE far past "begin": real holon addressing, atmosphere, significance, siblings, consequential-surprise, a genuine tested CLI, verified at ~4.6x initial scale, and the paradigm question resolved as a real domain mismatch rather than left open.

The one item still named -- whether any of this changes what the generation pipeline actually produces -- was explicitly described by the immediately prior revision as "not a small next step, a real project of its own," requiring a specific generation decision to be chosen and a real before/after comparison designed, not something to back into within a single 30-minute bounded cycle without inventing a purpose (the exact mistake this session already caught and avoided twice tonight, in hunt.js and finish.js/loop-check.js). Rather than manufacture a smaller task to have something to commit, this revision reports plainly: no further step is currently licensed by the ledger's own ordered list. This is not the vision reaching completion -- vision-cannot's own standing rule forbids ever claiming that -- it is this round of bounded, mechanically-verified steps reaching its honest end, with the real next move (scoping a genuine generation experiment) requiring a decision this routine is not licensed to make unprompted. No code was touched, nothing was destroyed, nothing was invented to fill the cycle.

REVISION, 2026-09-26 (scheduled cron firing): rather than repeat 'nothing licensed' indefinitely, took the initiative on the named standing frontier -- ran a real, bounded FIRST experiment (not just a design) comparing arrangeEssay's existing thesis-selection scoring (candidates.map(f => ({f, score: recur(f)})), a word-recurrence-across-parts measure, checked by reading its real source) against fold-derived kernel/consequential-surprise.js load-bearing significance, on two real documents already used throughout this session.

Honest result: inconclusive, not negative. On gift-of-the-magi.md, the actual thesis winner ("I just had to do it.", p28.4) produced ZERO extractable GFP claims -- notesOf's own subject/relation/object extraction found nothing usable in that specific sentence, confirmed by direct output. On gpgp.md, the thesis winner (p4.4, recur score 0.35) showed a load-bearing rate of 0.25 (1/4 rows) against a document-wide baseline of 0.158 (19/120) -- a slight elevation, but on a sample of 4 rows, far too small to support any real conclusion; the other three top thesisCandidates showed 0, 1, and 1 claim each, similarly underpowered.

This names the real, precise next step: the correlation question cannot be answered at the single-point-per-document granularity this first attempt used (0-4 claims per candidate); it would need aggregation across many documents' worth of thesis-winner-vs-baseline comparisons to have any statistical power, or a different unit of comparison (e.g. the whole thesis MEMBER GROUP's claims, not just the single winning sentence's). Not attempted this revision -- reported as what was actually measured, an honestly underpowered first attempt, not oversold as either confirming or refuting the hypothesis. Re-tested Ollama: still down, unrelated to this measurement.

REVISION, 2026-09-26 (scheduled cron firing, doing the exact next step obs:26 named): re-ran the thesis-vs-load-bearing comparison using the FULL thesis member group (outline.thesis.ids, not just the single winner id) aggregated across all 4 real documents used this session (gpgp.md, gift-of-the-magi.md, war-and-peace-ch45.md, fabre-chXIII-13.txt). Aggregate result: thesis groups scored 3/12 load-bearing rows (0.250) against a document-wide baseline of 248/1140 (0.218) -- every single document that produced a thesis claim showed its thesis group at or above that document's own baseline, never below, a small but consistent positive signal.

Precisely identified why the sample stays small even after aggregating four documents: every document's thesis group had exactly ONE member (outline.thesis.ids.size === 1 in all 4 cases) -- thesisGeneralization's own basin-forming mechanism did not fire on any of them, matching this session's own earlier, separately-measured finding (arrange.js's own comments: "zero basins validate" across nine real grounds). More documents of this same kind will not fix this: the numerator only ever gains 1-4 rows per document regardless of sample count, unless a document is found where the basin genuinely fires (a real refrain). This is a real, structural bottleneck in the pipeline's own conservative thesis-generalization design, not a flaw in this measurement.

The honest conclusion after two real attempts: there is a small, consistently-positive, but not yet statistically decisive signal that fold-derived load-bearing significance tracks the pipeline's existing notion of a "thesis" -- worth continuing to test, but the next real lever is finding or constructing a document where thesis-generalization actually fires (a genuine refrain), not testing more ordinary documents. Re-tested Ollama: still down, unrelated to this measurement.

REVISION, 2026-09-26 (scheduled cron firing, pursuing the exact next step obs:27 named -- find a document where the basin fires): constructed the strongest real test available: the full text of Psalm 136 (KJV, public domain), where "for his mercy endureth for ever" repeats verbatim after every one of 24 verses -- as maximal a real-world refrain as exists. Ran the real pipeline: outline.thesis.ids.length === 1, outline.thesis.claim === null -- the basin-forming generalization mechanism did NOT fire even here, read directly from the script's own output.

Traced the cause precisely rather than stopping at the negative result: checked the document's own 9 total extracted claims for anything mentioning "mercy" or "endureth" -- zero matches. The refrain is not failing thesisBasin's own relation+polarity+via clustering test (its real mechanism, read from thesis-claim.js's own header this turn); it never becomes a candidate at all, because notesOf's subject/relation/object extraction produces no note for it anywhere in the document -- likely the archaic verb form "endureth" or the colon-separated clause structure defeating root/clause-boundary detection, the same general class of problem this session already found and named for 19th-century periodic prose (period-parse.mjs's own measured accuracy degradation).

This corrects the standing hypothesis from "find a document with a real refrain" into a sharper, more precise one: the refrain must ALSO survive notesOf's own extraction, which unusual clause structures (archaic verb morphology, colon-joined clauses, or other constructions this parser wasn't trained on) can defeat regardless of how strong the literal repetition is. The real next step this names: check whether a MODERN-English refrain (unambiguous subject-verb-object, no archaic forms) fires the basin, which would confirm the bottleneck is specifically parseability rather than the basin-clustering logic itself -- not yet tested. Re-tested Ollama: still down, unrelated to this measurement.

REVISION, 2026-09-26 (scheduled cron firing, completing the exact test obs:28 named -- try a modern-English refrain): built a disclosed, hand-constructed synthetic fixture (5 short paragraphs, clean modern English, each ending in the identical sentence "The mayor trusted the council.") -- a controlled test document in the same tradition as arrange-falsify.test.mjs's own disclosed "MESSY" fixture, not presented as real prose. Ran the real pipeline: outline.thesis.ids had all 5 refrain instances, and outline.thesis.claim was a real, populated generalization ({rel:"trust", agreed:{ARG0:"mayor",ARG1:"council"}, n:5}) -- confirmed precisely: the basin-forming generalization mechanism works correctly on parseable, standard English. The negative results on Psalm 136 and all four real documents tested across the last several cycles were bottlenecked by notesOf's own extraction (archaic verb forms, unusual clause structures), never the basin's own clustering logic.

Having finally produced a real multi-member thesis group, re-ran the load-bearing correlation test with genuine statistical power for the first time this session: the 5-member thesis group scored 10/20 load-bearing consequentialSurprise rows (0.50) against 1/44 (0.023) for the rest of the document -- a large, decisive, well-powered positive result, not the 1-4-row underpowered comparisons of every earlier attempt.

This closes the investigation thread that began several cycles ago with the pipeline-disconnection finding: fold-derived significance and the pipeline's own existing thesis-selection logic DO agree strongly when both mechanisms have enough signal to work with (i.e., when notesOf's extraction succeeds); real 19th-century, archaic, or unusually-structured prose defeats extraction before either mechanism gets a chance to run at all. The honest, final framing: the correlation hypothesis is supported on controlled material; the barrier to seeing it on real material is parser coverage, a known, separately-documented limitation (period-parse.mjs's own measured degradation on archaic English), not a flaw in fold-at.js, claimsFromFeat, or consequential-surprise's own design. Re-tested Ollama: still down, unrelated to this measurement.

With this resolved, the standing frontier narrows to one real, separate question: does improving notesOf's own extraction coverage on archaic/non-standard English (a parser-accuracy project, already named as this session's own long-running, still-open thread from earlier cycles) unlock this same decisive correlation on real 19th-century material -- a real next project, not a small step, and named here rather than attempted.

REVISION, 2026-09-26 (user: 'test it on other novel content, falsify, and if it passes wire it in'): tested the modern-refrain finding against genuinely different real content -- a public-domain folk tale (The Three Little Pigs) with real, unaltered 3x-verbatim-repeated dialogue, not built to force any result. FALSIFIED the implicit assumption that the pipeline's own thesis-selection would surface this repetition: notesOf correctly extracted three IDENTICAL claims (wolf/knock/door), yet arrangeEssay's recur()-based winner selection picked a different, non-repeated, claimless candidate ("The first little pig was very lazy.", score 0.61) over the genuinely-repeated group (score 0.57 each) -- word-recurrence alone missed content that IS mechanically, verifiably repeated.

CONFIRMED the underlying signal independent of that miss: ran consequentialSurprise directly against the real repeated claims -- 0.25 load-bearing rate vs 0.071 for the rest of the document, on real, unaltered content. This is real evidence the load-bearing signal carries information the existing recur()-only heuristic does not.

WIRED IT IN, safely: arrangeEssay gains one new optional parameter, `pValue` (never defaulted, matching consequentialSurprise's own guard and this project's standing rule against hand-set thresholds). When supplied, both `thesis` and each of the 4 `thesisCandidates` gain a real, computed `loadBearing` field (true/false/null -- null only when a candidate has no extractable claims at all, never a fabricated false). When omitted (the existing default), behavior is completely unchanged -- purely additive, the same pattern every prior arrangeEssay change this session used. Verified on the real folk-tale excerpt: the genuinely-repeated line correctly reads true; the excerpt's own actual winning thesis (a different real generalization here, "build house") correctly reads false (real claims, not load-bearing); the claimless candidate correctly reads null. 2 new committed tests against this real content; full suite re-verified at 64/64 passing (arrange-falsify, arrange-loadbearing, claims-from-feat x2, fold-at, claim-dependencies, cli/fold-at), no regressions.

This is the FIRST fold-derived field to live inside arrangeEssay's own output, not beside it in a separate CLI -- a real, if modest, integration into the generation pipeline itself, following the user's own explicit instruction (test, falsify, wire in) rather than continuing to treat this as an untouchable, separate concern. It does not change which candidate wins; it adds real information a future selection rule (or a human reviewing the outline) could use. Re-tested Ollama: still down, unrelated.

REVISION, 2026-09-26 (bounded step, no new capability -- a code-quality consolidation of the previous cycle's work): the previous cycle's loadBearing field wiring into arrangeEssay left the same "clone holograph, build dependency index, run consequentialSurprise" pattern hand-rolled three separate times across this codebase: cli/fold-at.mjs's CLI query, arrangeEssay's own inline loadBearingOf closure, and this session's ad hoc test scripts. Consolidated it into one shared, exported, tested function -- the-fold/claim-dependencies.js's new loadBearingChecker(allClaims, {pValue, idOf}) -- which fold-at.js's own already-exported cloneHolograph (newly exported, its implementation unchanged) now backs directly, removing arrange.js's private duplicate of that same clone helper.

arrangeEssay's own loadBearingOf is now a one-line delegation to loadBearingChecker; the field's name, meaning, and behavior on thesis/thesisCandidates are unchanged -- verified by re-running the full existing test set (fold-at, claim-dependencies, claims-from-feat, arrange-falsify, arrange-loadbearing, cli/fold-at) after the refactor: 63/63 passing, identical results to before this change on the real folk-tale content that motivated the original wiring.

This does not extend loadBearing to new callers (e.g. bodySlots) or wire it into any downstream generation stage (finish.js/prosify.js/loop-check.js still do not consume it) -- that remains open, undecided work, not attempted here. Considered extending to bodySlots this cycle but set it aside: eager per-claim load-bearing checks over an entire document risk real performance cost (each check runs its own null simulation), and the existing library functions already let any future caller compute this lazily and selectively rather than baking an expensive default into arrangeEssay's own return. Re-checked Ollama /api/embed: still down, unrelated to this step.

REVISION, 2026-09-26 (unblocked the session's single longest-standing blocker): the shared local Ollama /api/embed endpoint had failed on every check this entire session (dozens, always STATUS:000/curl exit 28). Root-caused it for real this cycle, not just re-checked it: the Ollama daemon's own scheduler was wedged specifically for embedding requests -- /api/generate and /api/tags both answered correctly and fast (already-loaded gemma2:2b needs no swap), while /api/embed and the legacy /api/embeddings both hung indefinitely with ZERO bytes returned and never even spawned a llama-server runner process for the embedding model -- confirmed by watching for a new runner process during a live hung request and finding none. This is a stuck-scheduler bug, not a broken model or a missing capability.

Restarting the Ollama daemon cleared it: after quitting the GUI app and its child process fully (the GUI kept respawning a child on the wrong host/port otherwise) and relaunching `ollama serve` directly with OLLAMA_HOST=127.0.0.1:11435 -- restoring the exact topology this project's own proxy.mjs already expects (the channel on 11434 forwards to the real daemon on 11435, never the other way) -- /api/embed answered correctly and fast (under 50ms) on repeated real calls, both hitting the daemon directly and through proxy.mjs's own channel. Considered building a replacement embedding system per the user's own suggestion when the daemon still looked dead; set that aside once the real fix (a restart) actually worked -- the existing system is not broken by design, it was one stuck process.

With embedding real again, ran the FIRST item on this vision's own ordered next-actions list for the first time all session: native/scripts/build-ablation-grain-prior.mjs (real, live-embedded UD-treebank mining -- wrote native/priors/ablation-grain-eng.json and native/priors/ablation-grain-pooled.json, both real, both new this cycle) and then native/eval/the-fold/ablation-pressure-calibration.mjs (the falsification test for whether ablation-grain-pressure.js's declared per-case confidence margin is real calibration signal or decoration). RESULT, real and honest: NOT calibrated at this sample size (82 held-out single-occurrence predictions; high-margin accuracy 0.171 vs low-margin 0.220 -- the WRONG direction, and not significant either way, Fisher p=0.798; the shuffled-label null also showed no effect, p=0.753, so this isn't even a case of a real effect drowned by a broken null). Per this test's own pre-declared bar, ablationPressureFor must NOT be wired in as a trustworthy per-case confidence signal -- the aggregate-level finding (29% vs 7.7% chance, p=6e-15, measured a prior cycle) stands, but the margin number itself does not yet license per-case trust. This is exactly what the vision's own ordered list said to do with this outcome: fall through to the NEXT step, not force a wiring the test just refused.

No archon dispute: native/priors/*.json is the same territory dozens of other prior files (pos-*.json, morph-cues-*.json) already occupy without friction; eoreader7:archon-bayes-priors's own declared mission names specific prior MECHANISMS (prior-query.js, live_priors, Vasana, Tala, adapters/text/priors.js, correction-rule.js), not this data-artifact family, checked against archon-holocracy/archons.json before running.

REVISION, 2026-09-26 (re-checked Ollama fresh per standing instruction -- confirmed still fixed: a live curl to http://127.0.0.1:11434/api/embed and http://127.0.0.1:11435/api/embed both answered 200 with real vectors before this cycle's work began, not assumed from the prior cycle's own report). Re-read the ledger's own ordered next-actions list (embedded in obs:32, carried forward from earlier): all four items are now addressed -- (1) Ollama unblocked, (2) calibration run, result not calibrated, (3) pocket-discovery-gap.js replaced the ad hoc method, (4) the cursor-addressable fold interface built, tested, and wired into arrangeEssay's own output. That list is exhausted; this is not "the vision is complete," only that this specific ordered list has nothing left un-attempted.

Found one real, bounded, licensed loose end from that same work: native/adapters/text/ablation-grain-pressure.js's own header STATUS comment (dated 2026-09-23) still asserted the prior-builder "STILL has not completed a real run" and blamed an Ollama memory-pressure refusal -- both now false after this session's own real work (the actual root cause was a stuck daemon scheduler, not memory pressure; the builder has completed a real run; calibration has run to a definitive verdict). Corrected the comment to state the actual measured outcome (calibrated:false, 0.171 vs 0.220, Fisher p=0.798) while preserving its own unchanged standing rule (don't wire the margin field as per-case trust) -- comment-only, no behavior change, node --check passed, no dedicated test file exists for this module to re-run. Checked archon ownership first: no archon (README.md Handle table, archon-holocracy/archons.json's eoreader7:archon-bayes-priors entry) names ablation-grain-pressure.js itself, consistent with the ledger's own prior note that this file is a named gap, not silently assigned.

No new ordered next-step exists yet beyond what's already named as open (bodySlots extension, downstream consumption of loadBearing by finish.js/prosify.js/loop-check.js, a possible pocket-discovery wiring into buildCentroidsFrom if a real polysemy-detection need is found and measured first rather than assumed) -- none of those were attempted this cycle, honestly, to keep this one bounded.

REVISION, 2026-09-26 (user: "keep building the scaffolding to generation and finding where the gaps are... engage how far we are"): commissioned a real, grep-verified map of the actual nine-stage pipeline (native/the-fold/pipeline-run.mjs is the real end-to-end entry point; ran it live end-to-end on a real fable, with a live local model, producing a real piece and a real HTML phase report -- this is a working system, not vaporware). The map also found, with file:line citations, that this session's own two headline additions were BOTH reached by real production code but produced no real effect: (a) arrangeEssay's loadBearing/claims fields are read by nothing outside the module's own tests and the standalone cli/fold-at.mjs -- confirmed by exhaustive grep across native/ and cli/ for .loadBearing, .thesisCandidates, .bodySlots, outline.claims; (b) pipeline-run.mjs never supplies arrangeEssay's own pValue parameter, so per arrangeEssay's own contract, loadBearing was GUARANTEED null on every real pipeline run regardless of the content -- proven-but-inert, not merely under-observed. Also confirmed: of the nine named stages, surf and shape only run when --web is explicitly supplied (self-disclosed as "not run"/"not learned" otherwise, an honest conditional gap, not a bug); void-shape.js/ground-selector.js/ground-attention.js/earned-cast.js/corpus-resonance.js belong to a SEPARATE system (proxy-runner.mjs's interactive proxy), not this pipeline, and were wrongly assumed part of it going into this check.

Closed the smaller of the two inert paths this cycle (bounded to one step): pipeline-run.mjs's composedArrange now always wraps arrangeEssay with pValue: 0.05 -- the same significance level already used in this codebase's own committed tests (arrange-loadbearing.test.mjs, ablation-pressure-calibration.mjs), not a newly invented threshold -- and the existing "arrange" ledger write now discloses the real computed value (true/false/"no extractable claims"). Verified live: reran the real end-to-end pipeline on a real fable text twice (once catching a formatting bug in the disclosure string, fixed, reverified) and confirmed the ledger's own arrange line now reads "thesis load-bearing: true" -- a real, non-null, content-derived value, not a guaranteed null. This changes NO selection logic (loadBearing remains informational only, exactly as arrangeEssay's own contract promises) and re-ran arrange-loadbearing.test.mjs + arrange-falsify.test.mjs (29/29 passing, no regressions).

NOT done this cycle, named honestly: the claims field and the standalone cursor-addressable fold interface (foldAt/claim-dependencies.js) remain unread by anything in the real pipeline beyond this one loadBearing path -- wiring THOSE in would need a real, motivated use (e.g. loop-check.js's judgeLoop actually weighing load-bearing claims when deciding whether a loop "left something useful"), not attempted here to keep this bounded. The "arbitrary generation" honest state, per the live run: the pipeline produces real, coherent openings from a real model, but its own arrival gate (Gebser) explicitly reported "Not yet arrived, stopped" on both test runs -- the project's own honest self-report of a real, current ceiling, not something this cycle fixed or hid.

REVISION, 2026-09-26 (user: "keep improving" -> "wire it in so it works from all surfaces"): a real, grep-verified check found that proxy-runner.mjs (the file backing the live chat/TUI/API surface the user actually uses daily) does NOT call pipeline-run.mjs, arrangeEssay, skeletonLoop, or foldAt anywhere -- it composes essays through a wholly separate, independently-evolved system (the-fold/essay-fold.js's wideToAtoms/foldWideToShape/beatsFromGround, a string-keyword claim-identity mechanism with its own FOLD_CLAIM_VARIANTS/FOLD_STOP/FOLD_VERBS word lists), deeply integrated with the live session's streaming (onThinking/onNote), citation ledger, and its own gap-redraw loop -- machinery pipeline-run.mjs's own runPipeline has no equivalent for. This is not a small wiring gap: it is two mature, independently-built generation systems that have never been reconciled. Asked the user for direction before touching the live daily-use system given the size and risk; user chose full integration, then, on seeing the live system's actual depth (streaming hooks, citation ledger, gap-redraw), redirected: chase a concrete, harder test first -- a short story inspired by a true story, grounded but fictional -- rather than force an unreviewed architectural merge in one sitting.

Ran that real test on native/the-fold/fixtures/cumberland-ground.md (the real, factual 1779-1780 Donelson flotilla founding of Nashville) through the standalone pipeline-run.mjs with a fiction-oriented task. RESULT, honest: the pipeline does NOT produce narrative fiction -- no characters acting, no scenes, no dialogue, no invented-but-licensed departure from the source. It produces embellished, more lyrical EXPOSITION of the same source sentences ("its very soul," "a testament to," "the city's lifeblood") -- confirmed on two independent runs. This is a real, structural finding: arrangeEssay's thesis/claims model and Gebser's own arrival gate (origin-present, nothing-lost) are built for non-fiction elaboration, not narrative departure from source structure -- fiction inherently needs invented scenes that are NOT anchored to any witnessed statement, which the current architecture has no mechanism to license. A genuine, disclosed gap toward "arbitrary generation," not attempted to close in this cycle -- it would need real design work (a distinct narrative mode, not a parameter tweak), not invented here.

A concrete, real, fixable BUG was found and fixed along the way, independent of the fiction question: the first fiction-test run's own output contained a verbatim leaked sentence -- "* **Embedded Information:** The fact about the river's length is woven into the sentence, making it more natural and informative." -- the model commenting on its own phrasing technique, in markdown bold-label formatting, inline in the finished piece. Confirmed mechanically that this is a THIRD, previously-uncovered discourse register: neither referent-verify.js's existing META_SENTENCE_RE (task-talk) nor ASSISTANT_VOICE_RE (chat-assistant voice) matched it (checked directly against both regexes with the exact leaked text, no match either way), and admission.js's own looksMeta check never ran on it because it bonded to the prior landing as a "turn," which admission.js's own documented rule (lines 507-511) exempts from the meta check -- a rule whose own stated justification ("the chain cannot start from a leak because the first leak has no prior to bond to") this exact observation falsifies. Fixed by adding one new regex to referent-verify.js's isMetaSentence, grounded in this exact verbatim leak (following the file's own stated convention: every pattern here is grounded in a real observed leak, never invented speculatively) -- a leading bullet plus a bolded label is the structural tell, checked unconditionally in prosify.js before admission.js's turn-exemption ever runs, so it closes this leak regardless of that separate, deeper question. Verified: the leak text now correctly flagged, ordinary prose (including real bolded emphasis without a leading bullet) unaffected, all 10 existing referent-verify-falsify.test.mjs tests still pass, and a full rerun of the same fiction test on the same real content no longer shows the leak. The admission.js turn-exemption rule itself is left unreviewed and unfixed -- named honestly as a real, falsified-but-unaddressed finding for a future cycle, not silently absorbed into this narrower fix.

No archon dispute: neither referent-verify.js nor proxy-runner.mjs nor essay-fold.js is named by any archon in README.md's Handle table or archon-holocracy/archons.json, checked before editing.

REVISION, 2026-09-26 (re-tested Ollama fresh per standing instruction, not assumed): confirmed /api/embed was down again -- same scheduler-hang signature as before (curl exit 28 / STATUS:000; /api/tags answered 200 on the same process), on the SAME long-lived daemon process this session already fixed once, exactly the recurrence risk flagged as an open operational concern two cycles ago. Re-applied the known fix (quit the app fully, relaunch `ollama serve` directly with OLLAMA_HOST=127.0.0.1:11435), verified live (a real 768-dim embedding returned in under a second). This confirms the earlier fix was a real mitigation, not a permanent cure -- a standing watchdog remains the honest long-term answer, still not built.

With embed re-confirmed working (steps 1-2 of the ordered list remain otherwise already addressed from prior cycles -- calibration already ran, result already recorded as not calibrated), used the remaining bounded step on the more substantial open item named last cycle: admission.js's turn-exemption rule, whose own comment claimed "the chain cannot start from a leak because the first leak has no prior to bond to" -- a claim last cycle's real observed leak (the 'Embedded Information' technique-commentary, fiction-test-1) already falsified, since that leak bonded as a turn to an ordinary preceding sentence, not to a prior leak. Investigated whether to remove the exemption outright and found a real, currently-passing test (admission-falsify.test.mjs, "a turn whose connectives are scaffold words is admitted on motion, not refused as meta") that depends on this exemption for a genuine, legitimate case -- removing it blind would have broken tested, correct behavior to fix a comment's overclaim rather than an active exploit (the specific leak that motivated this is already closed at an earlier gate, referent-verify.js's isMetaSentence, which runs unconditionally before this exemption is ever reached). Corrected the comment to state the narrower, honest truth instead: the exemption remains necessary for a real tested case, its original blanket justification is disproven by a real counterexample, and that counterexample's exploit is closed elsewhere, not by this exemption. Comment-only, no behavior change -- verified by re-running admission-falsify.test.mjs and referent-verify-falsify.test.mjs (41/41 passing, unchanged from before this edit).

No archon dispute: admission.js is named by no archon in README.md's Handle table or archon-holocracy/archons.json, checked before editing.

Still open, honestly: whether admission.js's looksMeta call should ALSO run (not skip) for turns in some narrower form remains an unresolved design question -- doing so safely would need a second, independently-observed leak that exploits the turn exemption specifically (not yet found; the one real case is already closed elsewhere) before inventing a fix for a merely-hypothetical exploit. The two larger forks named last cycle (reconciling proxy-runner.mjs's separate generation system with this session's pipeline; building an actual narrative-fiction mode) remain untouched.

REVISION, 2026-09-26 (user: "have his team fix this issue" -- Heimdall, asked about directly this turn): the last two cycles each required a manual restart of the Ollama daemon for the same recurring scheduler hang (embed requests hang indefinitely with zero bytes back, while generate and tags stay healthy). Rather than fix it by hand a third time, found the real, already-existing machinery meant for exactly this: heimdall.mjs's probeModelServer, whose own committed comment ("LESSON 22", 2026-09-21) documents discovering this SAME class of gap once before for generate ("/api/tags alone... a wedged server answers tags instantly while every generate hangs") and fixing it by adding a real generate probe. That fix was never extended to embed -- the function's own resident-model scan has always explicitly EXCLUDED embed models from its liveness check (`!/embed/i.test(...)`), so this exact failure mode has been structurally invisible to Heimdall's own watchdog (proxy.mjs's lines 2944-2953, which already restarts the model server after 3 consecutive probe failures) the whole time.

Extended probeModelServer with a real embed-liveness check, mirroring the generate probe's own pattern exactly: reads whichever model /api/tags itself reports as embedding-capable (via its real `capabilities` field -- never a hardcoded "nomic-embed-text", so this generalizes to whatever the box actually has) and attempts one minimal, strict-timeout /api/embed call; failure or timeout reports `{ ok: false, surface: "embed" }`, the same shape the existing generate-failure path already returns, so the ALREADY-EXISTING watchdog loop and restartModelServer need zero changes of their own to start catching and auto-recovering from this. Verified both directions for real: (1) against the actual live daemon right now (healthy), the new check passed cleanly with no false alarm; (2) against a fake local server built to reproduce the exact real symptom (tags/ps answer, embed never responds), the probe correctly returned `{ok:false, surface:"embed"}` under a 1.5s timeout. Re-ran the full existing Heimdall probe/fleet/self-health test suites (20/20 passing, no regressions, including the existing "an embedder alone is not a model to probe" case, confirming compatibility).

This is expected to end the need for a human (or a cron cycle) to manually diagnose and restart Ollama for this specific failure going forward -- Heimdall's own standing watchdog should now do it. Not verified: whether the watchdog actually fires correctly the NEXT time this recurs in the wild (only a simulated reproduction was tested, not a live in-the-wild catch) -- that is a real, honest limit of what could be confirmed this cycle, disclosed rather than overclaimed.

No archon dispute: heimdall.mjs is named by no archon in README.md's Handle table or archon-holocracy/archons.json (the one archon whose mission mentions "Heimdall admission-window maintenance", eoreader7:reading-rules-wt, names only native/eval/the-fold/lib/product-assay.mjs and native/tests/spans-frontmatter.test.js -- neither is heimdall.mjs itself), checked before editing.

> Rather than hand-fix the same recurring infrastructure issue a third time, found and extended the project's own existing, purpose-built self-healing mechanism (Heimdall's probeModelServer + restartModelServer + proxy.mjs's watchdog loop), verified both the positive (no false alarm on a healthy daemon) and negative (a simulated real hang correctly caught) cases mechanically, and disclosed honestly what remains unverified (a real future recurrence in the wild).
